#include"Cjt_cites"

#include<iostream>
#include<sstream>
using namespace std;

int main(){
	string linia, m;
	getline(cin,lini);
	istringstream iss(linia);
	while (iss >> m and m != "sortir"){
		if (m == "afegir"){
			iss >> m;
			if (m == "text"){
				vector<string> llegir;
				getline.srt(linia);
				iss.seekg(13);
				while (iss >> m)
					llegir.push_back(m);
				llegir.erase([llegir.size() - 1]-1) 
				llegir_text();
			}
			else {
				
				
		
	